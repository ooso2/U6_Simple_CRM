<%= form_with(model: customer, local: true, html: { multipart: true }) do |form| %>
  ...
  <div class="field">
    <%= form.label :image %><br>
    <%= form.file_field :image, direct_upload: true %>
  </div>
<% end %>
